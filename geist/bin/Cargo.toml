[package]
name = "geist"
description = "geist.sh — governed runtime for autonomous agents"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[[bin]]
name = "geist"
path = "src/main.rs"

[dependencies]
geist-edge = { workspace = true }
tokio.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
serde_json.workspace = true

# Extension crates — linked here so inventory collects their registrations.
# The binary is the composition root: it names what it composes.
geist-access-control.workspace = true
